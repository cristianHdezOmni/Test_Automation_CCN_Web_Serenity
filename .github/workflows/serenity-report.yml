# name: Serenity BDD Report

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Java 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '17'

#       - name: Run tests and generate Serenity JSON
#         run: >
#           npx cucumber-js --require-module ts-node/register --require src/support/world.ts --require src/support/hooks.ts --require src/StepDefinitions/**/*.ts src/Resource/features/**/*.feature --format @serenity-js/cucumber --format-options "{\"outputDirectory\": \"target/site/serenity\", \"specDirectory\": \"src/Resource/features\"}"
#         continue-on-error: true

#       - name: Generate Serenity BDD HTML report
#         run: npx serenity-bdd run

#       - name: Upload Serenity report artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: serenity-report
#           path: target/site/serenity

#       - name: Deploy Serenity Report to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./target/site/serenity


name: Serenity BDD Report

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  REPORT_DIR: "./target/site/serenity"

jobs:
  Run_tests:
    name: "🧪 Pruebas (Run Tests)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Run tests and generate Serenity JSON
        run: >
          npx cucumber-js --require-module ts-node/register --require src/support/world.ts --require src/support/hooks.ts --require src/StepDefinitions/**/*.ts src/Resource/features/**/*.feature --format @serenity-js/cucumber --format-options "{\"outputDirectory\": \"$REPORT_DIR\", \"specDirectory\": \"src/Resource/features\"}"
        continue-on-error: true

      - name: Upload Serenity JSON artifact
        uses: actions/upload-artifact@v4
        with:
          name: serenity-json
          path: ${{ env.REPORT_DIR }}

  generate_report:
    name: "📊 Reporte (Generate Report)"
    runs-on: ubuntu-latest
    needs: Run_tests
    steps:
      - name: Download Serenity JSON artifact
        uses: actions/download-artifact@v4
        with:
          name: serenity-json
          path: ${{ env.REPORT_DIR }}

      - name: Generate Serenity BDD HTML report
        run: npx serenity-bdd run

      - name: Upload Serenity report artifact
        uses: actions/upload-artifact@v4
        with:
          name: serenity-report
          path: ${{ env.REPORT_DIR }}

  upload_report:
    name: "📦 Subir artefacto (Upload)"
    runs-on: ubuntu-latest
    needs: generate_report
    steps:
      - name: Download Serenity report artifact
        uses: actions/download-artifact@v4
        with:
          name: serenity-report
          path: ${{ env.REPORT_DIR }}

      # Este job podría realizar validaciones adicionales o subir a otro sistema si lo requieres.
      # Por ahora solo descarga el artefacto para el siguiente paso.

  deploy_report:
    name: "🚀 Deploy (Deploy)"
    runs-on: ubuntu-latest
    needs: upload_report
    steps:
      - name: Download Serenity report artifact
        uses: actions/download-artifact@v4
        with:
          name: serenity-report
          path: ${{ env.REPORT_DIR }}

      - name: Deploy Serenity Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ${{ env.REPORT_DIR }}